{
    "code-runner.executorMap": {
        "python": "cd $workspaceRoot && $pythonPath -u $fullFileName"
    },
    "code-runner.runInTerminal": true,
    "terminal.integrated.env.windows": {
        "PYTHONPATH": "${workspaceFolder}"
    }
}